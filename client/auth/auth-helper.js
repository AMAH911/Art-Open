import  {signout} from ".api-auth.js"

const auth = {
    isAuthenticated(){
        if(typeof window == "undefined"){
            
        }


        if(sessionStorage.getItem("jwt")){

        }
    }
}